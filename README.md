# Docker Compose Repo

This is where I store my homelab docker compose files. For obvious reasons, `.env` files are not stored here

Files generated by [docker_compose_generator](https://github.com/espege/home_server/tree/main/roles/docker_compose_generator)